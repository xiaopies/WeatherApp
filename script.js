// document.addEventListener('DOMContentLoaded', function() {
//     const sun = document.querySelector('.sun');
//     const hours = new Date().getHours();
//     // Adjusting the size of the sun based on the time (just an example)
//     let size = Math.max(20, 100 - (hours - 6) * 5); // Shrinks as time progresses after 6 AM
//     sun.style.width = `${size}px`;
//     sun.style.height = `${size}px`;
//     sun.querySelector('.temp').style.lineHeight = `${size}px`; // Keep the temperature vertically centered
// });
